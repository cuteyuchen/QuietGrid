# 回测数据目录

回测中心会枚举本目录中的 `.csv` 文件。每份数据至少需要以下列：

```text
timestamp,open,high,low,close
```

约束：

- `high`、`low`、`close` 必须是有限正数；
- 数据必须按时间升序排列；
- 观察期行数必须小于总行数；
- 文件名会作为控制台中的数据集名称；
- 前端不能访问本目录之外的路径。

仓库自带的 `example_klines.csv` 仅用于链路验证，不代表真实市场或策略绩效。
