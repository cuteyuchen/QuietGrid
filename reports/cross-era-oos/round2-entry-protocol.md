# Round 2：ETH 因果入口过滤 Development 协议

生成日期：2026-07-23

## 证据与假设

跨周期 Development 中：

- `X0_BASELINE` 六种子平均 PnL 为 -20.5400 USDT；
- BTCUSDT 六种子合计 PnL 为 +0.4306 USDT；
- ETHUSDT 六种子合计 PnL 为 -123.6707 USDT；
- P3/P4/P9 及两个组合退出防御全部比基线更差。

因此本轮不再调整退出阈值。唯一预注册假设是：现有 ETH 入口过滤器 `de0.50_ve1.05_rr0.25` 在 2020–2022 市场中过宽，使用开仓前 180 根已闭合 K 线的因果特征进一步收紧，可能过滤负期望窗口。

## 保持冻结

- 仅使用跨周期数据的 Development 108 个窗口；
- Validation 与 Final OOS 不计算收益、标签或候选指标；
- BTC 参数和入口逻辑不变；
- ETH 网格参数、库存、wind-down、费用、成交概率和退出逻辑不变；
- 方向保持 NEUTRAL；
- 固定种子：3、10、17、31、59、97；
- 不修改杠杆、网格层数、范围、最小步长或止损。

## 预注册搜索空间

只允许比现有 ETH 过滤器更严格或相同：

```text
max_directional_efficiency: 0.25, 0.35, 0.45, 0.50
max_volatility_expansion:   0.75, 0.90, 1.00, 1.05
min_reversal_ratio:         0.25, 0.35, 0.45, 0.55
```

共 64 个候选。不得在查看结果后扩展或微调本轮网格。

## Development 门槛

候选必须同时满足：

1. 6/6 种子组合净收益为正；
2. 六种子最差组合净收益为正；
3. BTC 与 ETH 六种子合计净收益均为正；
4. 六个种子的组合 Profit Factor 均大于 1；
5. 最差 5% 窗口平均损失相对当前基线改善至少 20%；
6. 最大回撤不高于 5%；
7. 最佳窗口集中度不高于 35%；
8. 至少保留当前基线 ETH 已交易窗口的 25%；
9. 手续费/毛利润不高于基线的 1.25 倍；
10. 六种子平均净收益为正。

若多个候选通过，按以下顺序选择唯一候选：

1. 最差 5% 窗口改善更高；
2. 六种子最差净收益更高；
3. 六种子平均净收益更高；
4. ETH 交易覆盖率更高；
5. filter_id 字典序。

若无候选通过，停止本轮，Validation 与 Final OOS 保持封存，生产参数不变。

若存在候选，下一步必须先单独实现 Validation BASE+COST50 阶段门禁，不能直接打开 Final OOS。
