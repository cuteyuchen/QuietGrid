# Round 3：BTC + ETH 联合因果入口过滤 Development 协议

生成日期：2026-07-23

## 证据与单一假设

Round 2 的 `de0.35_ve1.00_rr0.55` ETH 过滤器在跨周期 Development 上：

- 6/6 种子组合净收益为正；
- 六种子平均 PnL 为 +8.5554 USDT；
- 六种子最差 PnL 为 +1.6450 USDT；
- ETH 六种子合计 PnL 为 +50.9036 USDT；
- ETH 交易覆盖率为 26.03%；
- 通过 10 项门槛中的 9 项；
- 唯一失败项为最大回撤 5.54% > 5.00%。

该候选的 BTC 仍无额外入口过滤，六种子合计 PnL 约为 +0.43 USDT。本轮唯一假设是：固定 Round 2 的 ETH 过滤器，只增加 BTC 因果入口过滤，可以降低组合回撤并改善 BTC 收益稳定性。

## 保持冻结

- 只使用跨周期 Development 108 个窗口；
- Validation 与 Final OOS 不计算收益、标签或候选指标；
- 固定 ETH 过滤器：`de0.35_ve1.00_rr0.55`；
- BTC/ETH 网格、库存、wind-down、方向、费用、成交概率和退出逻辑全部不变；
- 固定种子：3、10、17、31、59、97。

## BTC 预注册搜索空间

```text
max_directional_efficiency: 0.40, 0.45, 0.50, 0.55
max_volatility_expansion:   0.90, 0.95, 1.00, 1.05
min_reversal_ratio:         0.25, 0.30, 0.35
```

共 48 个候选。该范围来自仓库既有 BTC 入口诊断的因果特征域，不在本轮结果后扩展。

## Development 门槛

候选必须同时满足：

1. 6/6 种子组合净收益为正；
2. 六种子最差组合净收益为正；
3. BTC 与 ETH 六种子合计净收益均为正；
4. 六个种子的组合 Profit Factor 均大于 1；
5. 最差 5% 窗口平均损失相对原始跨周期基线改善至少 20%；
6. 最大回撤不高于 5%；
7. 最佳窗口集中度不高于 35%；
8. ETH 至少保留原始基线已交易窗口的 25%；
9. BTC 至少保留原始基线已交易窗口的 25%；
10. 手续费/毛利润不高于原始基线的 1.25 倍；
11. 六种子平均净收益为正。

若多个候选通过，依次按尾部改善、最差种子 PnL、平均 PnL、BTC 覆盖率、filter_id 选择唯一候选。

无候选通过时停止，Validation 与 Final OOS 保持封存，生产参数不变。有候选通过时，只允许进入单独的 Validation BASE+COST50 门禁。
